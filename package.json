{
  "name": "build-case-notes-site",
  "version": "1.0.0",
  "private": true,
  "description": "Root build and local server for the case notes website.",
  "scripts": {
    "build": "node scripts/build-case-pages.js",
    "start": "node scripts/start-server.js"
  }
}
